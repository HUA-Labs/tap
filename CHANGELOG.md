# Changelog

All notable changes to tap will be documented in this file.

## Unreleased

### Added

- Public repo sync for `channels/` with modular `tap-comms` split
- Public repo sync for `bridges/` with Codex and Gemini runtime adapters
- Public repo sync for `scripts/windows/` launcher and bridge reference implementation
- Public repo sync for launch, bridge, cross-platform, and multi-device docs
- Config examples and Codex review/bootstrap memory docs under `configs/`

### Notes

- `channels/` is the protocol core
- `bridges/` is optional runtime-specific delivery
- `scripts/windows/` is the current Windows reference implementation
