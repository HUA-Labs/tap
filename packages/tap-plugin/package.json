{
  "name": "@hua-labs/tap-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server plugin for tap-comms multi-session orchestration",
  "scripts": {
    "test": "vitest run --environment=node --pool=threads --maxWorkers=1",
    "test:watch": "vitest --environment=node --pool=threads --maxWorkers=1",
    "test:validate": "tsx channels/__tests__/tap-validate.ts",
    "type-check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1"
  }
}
